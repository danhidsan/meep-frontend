import styled from 'styled-components';
import SortedIcon from '@/components/SortedIcon';

export const Container = styled.div`
  display: flex;
  flex-direction: column;
`;

export const Sorted = styled(SortedIcon)`
  margin-left: 5px;
`;
